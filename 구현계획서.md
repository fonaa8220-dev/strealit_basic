# 구현계획서

## 대상

- `app2.py`의 OpenAI 채팅 화면

## 목표

- 사용자가 화면에서 OpenAI API 키를 입력할 수 있게 한다.
- 사용할 API 키가 없으면 채팅 요청이 실행되지 않게 한다.
- 입력한 키가 화면이나 채팅 기록에 노출되지 않게 한다.

## 구현 계획

1. Streamlit API 레퍼런스에서 비밀번호 형식 입력란과 채팅 입력란의 사용법을 확인한다.
2. 채팅 화면의 사이드바에 비밀번호 형식의 API 키 입력란을 추가한다. 입력값은 현재 Streamlit 세션에서만 사용하고 SQLite에는 저장하지 않는다.
3. 사용자가 입력한 키를 우선 사용하고, 입력하지 않은 경우에는 기존 `OPENAI_API_KEY` 환경변수를 사용한다.
4. 두 곳 모두 키가 없으면 안내 문구를 표시하고 채팅 입력을 비활성화한다. 키가 있을 때만 `OpenAI(api_key=...)`로 응답을 요청한다.
5. 키 유무에 따른 화면 상태와 Python 구문을 확인한다. 실제 API 호출은 유효한 키가 준비된 경우에만 확인한다.

## 실행 조건

- 사용자가 이 계획의 실행을 명시적으로 요청한 뒤에만 `app2.py` 수정과 검증을 진행한다.

---

## 채팅 UI 시안 3종 제작 계획

### 목표

- 현재 `app2.py`의 채팅, API 키 입력, 첨부 파일, 기록 보기 기능을 기준으로 귀여운 화면 시안 3개를 만든다.
- 사용자가 비교하기 쉽도록 같은 화면 크기와 동일한 주요 요소로 보여준다.

### 시안 방향

1. **파스텔 노트**: 크림색 배경, 둥근 카드, 작은 스티커와 손글씨 느낌의 제목.
2. **말랑 캐릭터**: 작은 마스코트, 말풍선 모양의 대화창, 부드러운 파스텔 색상.
3. **캔디 팝**: 선명한 포인트 색상, 둥근 버튼, 경쾌한 배지와 아이콘.

### 제작 단계

1. 세 시안을 이미지 또는 화면 목업으로 제작한다. 각 시안에 채팅 영역, API 키 입력 상태, 첨부 버튼, 기록 이동 요소를 배치한다.
2. 세 시안을 한눈에 비교할 수 있게 제시하고 색상·분위기·구현 난이도를 짧게 설명한다.
3. 사용자가 시안을 선택하면, 별도 구현 계획을 이 문서에 작성한 뒤 `app2.py` 적용을 진행한다.

### 실행 조건

- 사용자가 시안 제작 실행을 명시적으로 요청한 뒤에만 시안을 만든다. 이번 단계에서는 `app2.py`를 수정하지 않는다.

---

## 2번 시안 수정 계획

- 대상: 위에서 제작한 **말랑 캐릭터** 시안만 수정한다.
- 토끼 캐릭터와 채팅 아바타를 웰시코기로 알아볼 수 있는 귀여운 캐릭터로 바꾼다.
- 메인 제목을 `ChatChat`으로 바꾸고, 기존 `함께 이야기해요` 및 토끼 관련 설명을 제거한다. 상단의 기존 브랜드 문구도 정리해 `ChatChat` 이름만 사용한다.
- 색상, 대화 구성, API 키 상태, 첨부 버튼 등 나머지 화면 요소는 유지한다.
- 수정한 2번 시안을 다시 보여주고, `app2.py`는 수정하지 않는다.

### 실행 조건

- 사용자가 이 수정 계획의 실행을 명시적으로 요청한 뒤에만 시안을 변경한다.

---

## 2번 시안 `app2.py` 적용 계획

### 적용 범위

- 채팅 화면 이름을 `ChatChat`으로 바꾸고 웰시코기 마스코트를 표시한다.
- 2번 시안의 민트색·살구색·둥근 말풍선 분위기를 채팅 화면, 키 입력란, 첨부 버튼에 적용한다.
- 기존 채팅, API 키 입력, 파일 첨부, 기록 보기 기능은 계속 동작하게 한다.

### 실행 단계

1. 필요한 Streamlit API 문서를 확인하고 현재 코드의 화면 구성과 변경 지점을 확인한다.
2. 작은 웰시코기 SVG를 추가해 제목 영역과 assistant 아바타에 사용한다.
3. `app2.py`의 제목과 문구를 `ChatChat`으로 바꾸고, 화면에 필요한 최소한의 스타일을 추가한다.
4. Python 구문, 변경 diff, 키 유무에 따른 입력 상태와 화면 렌더링을 확인한다.

### 실행 승인

- 사용자가 “2번시안으로 시행해”라고 요청했으므로 이 계획 작성 후 위 단계를 실행한다.

---

## API 키 팝업 변경 계획

### 목표

- API 키 입력란과 키 상태 문구를 사이드바에서 제거한다.
- 채팅 화면 상단의 `API 키 등록` 버튼을 누르면 팝업에서 키를 입력하거나 변경할 수 있게 한다.
- 팝업의 배경·모서리·버튼 색을 현재 `ChatChat`의 민트·살구색 디자인과 맞춘다.
- 입력한 키는 현재 Streamlit 세션에서만 사용하고 SQLite에 저장하지 않는다.
- 화면 입력 키가 없으면 기존 `OPENAI_API_KEY` 환경변수를 사용하고, 둘 다 없으면 채팅 입력을 비활성화한다.

### 실행 단계

1. Streamlit `st.dialog`와 비밀번호 입력란의 API 문서를 확인한다.
2. `app2.py`에 API 키 입력 팝업과 상단 버튼을 추가하고, 사이드바의 키 입력란과 상태 문구를 제거한다.
3. 팝업에서 등록한 키를 세션 상태에 보관하고 기존 OpenAI 요청에 사용한다. 키 변경 및 입력 키 제거도 팝업에서 처리한다.
4. 키 없음·입력 키·환경변수 키 상태에서 채팅 활성화 여부를 확인하고 구문과 diff를 검사한다.

### 실행 조건

- 사용자가 “키팝업도 디자인과 어울리게 실행해”라고 요청했으므로 이 계획을 실행한다.

---

## 텍스트 채팅 전용 변경 계획

### 목표

- 이미지 업로드와 파일 업로드 버튼, 팝업, 첨부 미리보기를 제거한다.
- OpenAI에는 사용자가 입력한 텍스트만 보내고, 기존 채팅·API 키 팝업·기록 보기 기능은 유지한다.
- 이전 대화 데이터와 SQLite 테이블 구조는 유지한다. 기존 첨부 파일과 기록은 삭제하지 않는다.

### 실행 단계

1. Streamlit 채팅 입력과 OpenAI 텍스트 입력 방식의 문서를 확인한다.
2. `app2.py`에서 업로드 관련 상수·팝업·세션 상태·파일 저장·첨부 표시·인코딩 코드를 제거한다.
3. SQLite의 기존 `files` 열은 유지하되 새 메시지는 빈 값으로 저장하고, 채팅 화면에서는 텍스트만 표시한다.
4. `app2_history.py`에서도 첨부 표시를 제거해 과거 내역을 텍스트 중심으로 보여준다.
5. 구문과 diff를 확인하고, 실제 화면에서 업로드 버튼이 사라졌는지와 텍스트 채팅 입력 상태를 확인한다.

### 실행 조건

- 사용자가 이 계획의 실행을 명시적으로 요청한 뒤에만 코드를 수정한다.

---

## 채팅 세션별 대화 및 보관 한도 계획

### 기준

- 보관 한도는 로그인한 사용자별로 적용한다. 사용자마다 최대 10개의 채팅 세션을 저장한다.
- 사용자 메시지 1개와 AI 답변 1개를 1회 대화로 센다. 채팅 세션마다 최근 100회 대화만 DB에 남긴다.
- 101번째 대화가 완료되면 해당 세션의 가장 오래된 1회 대화(사용자 메시지와 AI 답변)를 삭제한다.
- 새 채팅 세션을 만들 때 사용자의 세션이 10개를 넘으면 가장 오래된 세션과 그 메시지를 삭제한다.

### 데이터 변경

1. SQLite에 사용자별 채팅 세션 테이블을 만들고, 메시지에 세션 ID를 추가한다.
2. 기존 사용자별 메시지는 사용자마다 하나의 기존 세션으로 연결한다. 이전에 소유자가 확인되지 않아 숨겨 둔 메시지는 계속 분리해 둔다.
3. 기존 데이터가 한도를 초과하면 각 세션의 최근 100회 대화와 사용자별 최근 10개 세션만 남긴다.
4. 메시지 저장과 한도 초과 기록 삭제를 같은 DB 작업에서 처리해 한도가 일관되게 유지되도록 한다.

### 화면과 동작

1. 채팅 화면에 `새 채팅` 버튼과 세션 선택 목록을 추가한다. 새 세션에서는 이전 세션의 응답 ID를 사용하지 않는다.
2. 세션을 바꾸면 그 세션의 메시지와 마지막 응답 ID를 불러온다.
3. 과거 채팅 내역 화면에서도 세션을 선택해 해당 세션의 기록을 조회할 수 있게 한다.
4. 기존 `채팅 기록 삭제`는 로그인한 사용자의 채팅 세션과 메시지 전체 삭제로 유지한다.

### 검증

- 100회와 101회 대화에서 저장된 메시지 수와 가장 오래된 대화 삭제를 확인한다.
- 10개와 11개 세션에서 가장 오래된 세션 및 메시지 삭제를 확인한다.
- 사용자별 세션·메시지·응답 ID가 섞이지 않는지, 기존 DB가 정상적으로 연결되는지 확인한다.

### 실행 조건

- 사용자가 이 계획의 실행을 명시적으로 요청한 뒤에만 코드를 수정한다.

---

## 사용자별 채팅 구분을 위한 로그인 페이지 계획

### 로그인 방식

- 닉네임과 비밀번호로 계정을 등록하고 로그인하는 페이지를 만든다.
- `users` 테이블에 닉네임과 솔트가 적용된 비밀번호 해시를 저장한다. 비밀번호 원문은 DB나 채팅 기록에 저장하지 않는다.
- 로그인 화면에 이 앱이 학습용 예제이며 계정 잠금·비밀번호 재설정 등 운영용 보안 기능이 없으므로 민감한 정보를 입력하지 말라는 경고를 표시한다.

### 채팅 데이터 분리

1. SQLite `messages` 테이블에 사용자 닉네임 열을 추가한다. 기존 DB에는 실행 시 필요한 열을 추가하되 기존 행과 첨부 파일은 삭제하지 않는다.
2. 새 메시지는 로그인한 닉네임과 함께 저장한다. 채팅 화면과 과거 내역에서는 해당 닉네임의 메시지만 불러온다.
3. 채팅 이어가기용 마지막 응답 ID와 “채팅 기록 삭제”도 현재 닉네임의 기록에만 적용한다.
4. 기존 사용자 구분 정보가 없는 메시지는 어느 닉네임에도 자동으로 연결하지 않아 다른 사용자에게 노출되지 않게 한다.

### 화면과 세션

1. 로그인 전에는 로그인·계정 등록 화면만 보여주고, 올바른 닉네임과 비밀번호를 입력하면 채팅·과거 내역 페이지로 이동한다.
2. 로그아웃 버튼을 추가하고, 로그아웃할 때 현재 세션의 닉네임·채팅 상태·팝업에서 입력한 API 키를 지운다.
3. 로그인 페이지와 경고 문구를 기존 `ChatChat`의 웰시코기·민트색 디자인에 맞춘다.

### 검증

- 새 계정 등록, 올바른 비밀번호 로그인, 잘못된 비밀번호 거부를 확인한다.
- 서로 다른 계정의 대화와 삭제 범위가 분리되는지 확인한다.
- 로그아웃 후 이전 사용자의 채팅과 API 키가 세션에 남지 않는지 확인한다.
- 기존 DB를 읽을 수 있는지, 로그인 화면에 보안 경고가 보이는지 확인한다.

### 실행 조건

- 사용자가 이 계획의 실행을 명시적으로 요청한 뒤에만 코드를 수정한다.

---

## ChatChat 화면 디자인 개선 계획

### 디자인 방향

- 현재의 민트 배경, 살구색 포인트, 웰시코기 캐릭터를 유지한다.
- 짙은 녹색 글자, 정돈된 여백, 얇은 테두리와 부드러운 그림자로 화면의 완성도를 높인다.
- 버튼·입력란·카드·알림의 모서리와 색상 규칙을 통일한다.

### 화면별 변경

1. 로그인·계정 만들기: 웰시코기와 `ChatChat` 제목을 중심에 배치하고, 탭과 입력란을 하나의 카드처럼 정리한다. 비밀번호 조건과 보안 경고를 읽기 쉽게 표시한다.
2. 채팅: 상단 제목·API 키 버튼·과거 내역 이동을 간결하게 정렬한다. 빈 채팅에는 시작 안내를 넣고, 사용자와 AI 말풍선의 구분 및 대화 수 표시를 다듬는다.
3. 사이드바: 새 채팅, 세션 선택, 기록 삭제, 로그아웃의 우선순위가 보이도록 간격과 버튼 스타일을 정리한다.
4. 과거 내역: 세션 선택과 정렬·역할 필터를 한눈에 찾을 수 있게 배치하고, 메시지 목록의 간격과 색을 채팅 화면과 맞춘다.
5. API 키 팝업: 현재 디자인을 유지하면서 제목·설명·입력란·저장 버튼의 시각적 흐름을 정리한다.
6. 좁은 화면에서도 제목, 버튼, 입력란이 겹치지 않도록 반응형 간격을 적용한다.

### 구현·확인

- `assets/chatchat.css`의 공통 색상·간격·컴포넌트 스타일을 정리하고, 필요한 범위에서 `app2.py`와 `app2_history.py`의 화면 배치를 조정한다.
- 로그인, 키 팝업, 빈 채팅, 메시지가 있는 채팅, 세션 선택, 과거 내역을 실제 Streamlit 화면에서 확인한다.
- 구문 검사와 기존 로그인·세션 기능의 동작 확인을 수행한다.

### 실행 조건

- 사용자가 이 계획의 실행을 명시적으로 요청한 뒤에만 화면 코드를 수정한다.
